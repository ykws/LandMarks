name: Swift

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@master
    - name: Build and test
      run: xcodebuild clean test -project LandMarks.xcodeproj -scheme LandMarks -destination "platform=iOS Simulator"
